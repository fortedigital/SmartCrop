@inherits System.Web.Mvc.WebViewPage<Forte.SmartFocalPoint.Models.ViewModels.SmartFocalPointAdminPluginViewModel>
@using System.Web.Mvc.Html
@using EPiServer.Framework.Web.Resources
@using System.Web
@using EPiServer
@using EPiServer.Shell

@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <title>Title</title>
    <link href="@Paths.ToShellClientResource("ClientResources/epi/themes/legacy/ShellCore.css")" rel="stylesheet" type="text/css">
    <link href="@Paths.ToShellClientResource("ClientResources/epi/themes/legacy/ShellCoreLightTheme.css")" rel="stylesheet" type="text/css">
    <link href="@UriSupport.ResolveUrlFromUIBySettings("App_Themes/Default/Styles/system.css")">
    <link href="@UriSupport.ResolveUrlFromUIBySettings("App_Themes/Default/Styles/ToolButton.css")">
</head>
<body id="body">
    <div class="epi-contentContainer epi-padding">
    <div class="epi-contentArea">
        <h1 class="epi-prefix">Enable/Disable SmartFocalPoint connection for all images</h1>
        <div id="innerBody">
            Disabling this will disable connection to Azure services<br />
            and unset FocalPoint properties will remain unset.<br /><br />

            @using (Html.BeginForm("Action", "SmartFocalPointAdminPlugin"))
            {

                <text>Enable connection:</text> @Html.CheckBox("IsSmartFocalPointEnabled")<br /><br />
                <div class="epi-buttonContainer">
                    <span class="epi-cmsButton">
                        <input id="btnSave" class="epi-cmsButton-text epi-cmsButton-tools epi-cmsButton-Save" value="Save" type="submit">
                    </span>
                </div>
            }
        </div>
    </div>
    </div>
</body>
</html>